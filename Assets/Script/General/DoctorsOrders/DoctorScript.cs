using UnityEngine;

public class DoctorScript : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }


    // new orders come in after a timer that runs after last order time runs out or is fulfilled


    // create list of tools, randomly choose tools -- scalpel, saw, syringe, 

    // list of body parts

}
